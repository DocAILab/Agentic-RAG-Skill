schema_version: 1
runtime_id: component.rewrite.hyde
kind: component
version: 0.1.0
mutable: false

runtime:
  type: python-component
  path: scripts/component.py
  callable: run

provides:
  rewriter:
    input: RewriteRequest
    output: RewriteResult

requires:
  retriever:
    components:
      - component-vector-retriever
